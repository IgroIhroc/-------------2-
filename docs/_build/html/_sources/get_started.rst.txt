Get Started
===========

Цей розділ описує процес встановлення та запуску документаційного проєкту на базі Sphinx.

Вимоги
------

Перед початком переконайтесь, що у вас встановлено:

- Python 3.8 або новіше
- pip (менеджер пакетів Python)
- Git (опціонально)

Встановлення
------------

1. Клонуйте репозиторій:

.. code-block:: bash

    git clone <посилання_на_репозиторій>
    cd <назва_папки>

2. Встановіть необхідні залежності:

.. code-block:: bash

    pip install -r requirements.txt

Якщо файл requirements.txt відсутній, встановіть Sphinx вручну:

.. code-block:: bash

    pip install sphinx

Запуск документації
------------------

Для локального перегляду документації виконайте:

.. code-block:: bash

    make html

Після виконання команди відкрийте файл:

.. code-block:: text

    _build/html/index.html

Автоматичне оновлення (опціонально)
-----------------------------------

Для автоматичної перебудови документації при зміні файлів використовуйте:

.. code-block:: bash

    pip install sphinx-autobuild

    sphinx-autobuild . _build/html

Після запуску відкрийте у браузері:

.. code-block:: text

    http://127.0.0.1:8000

Структура проєкту
-----------------

- section1.rst – Розділ 1
- section2.rst – Розділ 2
- section3.rst – Розділ 3
- section4.rst – Розділ 4
- sources.rst – Список використаних джерел